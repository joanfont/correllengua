name: Test

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    name: "Build and test"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build
        uses: docker/build-push-action@v6
        with:
          tags: correllengua:test
          build-args: |
            ENV=test
            UID=1000
            GID=1000

      - name: Run PHP CS Fixer (check only)
        run: |
          docker run --rm \
            correllengua:test \
            ./vendor/bin/php-cs-fixer fix --dry-run --diff --verbose --using-cache=no

      - name: Run PHPUnit tests
        run: |
          docker run --rm \
            -e APP_ENV=test \
            correllengua:test \
            php vendor/bin/phpunit --do-not-cache-result

  build-and-push-prod:
    name: "Build and push for production"
    runs-on: ubuntu-latest
    needs:
      - build-and-test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          push: true
          tags: | 
            ghcr.io/${{ github.repository_owner }}/correllengua:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/correllengua:latest
          build-args: |
            ENV=prod
            UID=1000
            GID=1000
